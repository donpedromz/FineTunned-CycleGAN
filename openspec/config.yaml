# SDD Configuration — FineTunned-CycleGAN
# Auto-generated by sdd-init

context: |
  Python 3.12 project managed with uv. Currently a scaffold/skeleton —
  single dependency (kagglehub), no ML framework installed yet. Project
  name suggests CycleGAN for image translation; intended for Kaggle
  dataset integration. No tests, no CI, no linters configured.

strict_tdd: false

testing:
  runner:
    command: ""
    framework: ""
    available: false
  layers:
    unit: { available: false, tool: "" }
    integration: { available: false, tool: "" }
    e2e: { available: false, tool: "" }
  coverage:
    available: false
    command: ""
  quality:
    linter: { available: false, command: "" }
    type_checker: { available: false, command: "" }
    formatter: { available: false, command: "" }

phases:
  propose:
    enabled: true
  spec:
    enabled: true
  design:
    enabled: true
  tasks:
    enabled: true
  apply:
    enabled: true
  verify:
    enabled: true
  archive:
    enabled: true
